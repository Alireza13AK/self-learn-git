# installer / lancer
npm install
npm run dev

# git
git status
git branch
git fetch origin
git checkout -b DevAlireza
git rebase main
git reset --soft main
git commit -m "..."
git push --force-with-lease
